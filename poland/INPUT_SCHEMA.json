{
  "title": "Polish ministry of health COVID-19",
  "description": "This actor scrapes data about COVID-19 from the Polish ministry of health.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "email": {
      "title": "Email for alerts",
      "type": "string",
      "description": "Email where we should send email if there are no data about regions, which means something is wrong.",
      "prefill": "example@example.com",
      "editor": "textfield"
    }
  },
  "required": ["email"]
}
